## 1. Backend Server-Side Email API

- [x] 1.1 Implement `app/api/contact/route.ts` with validation, honeypot spam protection, and serverless email delivery pipeline to `luizltisistemas@gmail.com`.

## 2. Frontend 1-Click Submission UI

- [x] 2.1 Refactor `components/sections/Contact.tsx` to submit asynchronously via `fetch("/api/contact")`, display active loading spinner during submission, and render an instant on-page Success Screen with summary and optional WhatsApp accelerator.

## 3. Verification & Build

- [x] 3.1 Execute `npm run build` to verify backend route compilation, zero TypeScript errors, and successful build.
